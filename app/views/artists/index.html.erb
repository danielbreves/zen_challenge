<h1>Artists</h1>

<table>
<% @artists.each do |artist| %>
  <tr>
    <td><%= artist.name %></td>
    <td><%= link_to 'Show', artist %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to "Home", {:controller => 'home'} %>
